# Sync 2026-09-04 (run 12)

*Uitgevoerd volgens [synchronisatie-runbook.md](synchronisatie-runbook.md), in
één doorlopende sessie van 4 september 22:17 tot 5 september ~11:00.
Wat er misging en wat eraan te doen valt staat apart in
[sync-2026-09-04-leerpunten.md](sync-2026-09-04-leerpunten.md) — dertien punten.*

Volgorde: preview → lokaal → wijzigingsspoor → markeren → prod → vth → i2a →
vectorlaag → doorwerking → hertaling → onderwerp-as → MER → verificatie →
downstream → nazorg.

---

## Stap 0 — Preview

```
p2p    TE LADEN: 5 nieuw + 11 nieuwe versie = 16   (prod mist er 16)
       VERDRONGEN: 11    VERDWENEN: 12
vth    TE LADEN: 7 open dagen, samen 6.293 kennisgevingen
embed  TE EMBEDDEN: 0 regelingen (dirty-set vóór het laden)
```

Per documenttype: 6 omgevingsplannen, 4 omgevingsvisies, 3 programma's, 1
omgevingsverordening, 1 waterschapsverordening, 1 voorbeschermingsregels.

**Vier van de elf nieuwe versies dateerden van 4/5 augustus**, terwijl de
p2p-sweep van run 11 (28-08) afsloot op 381 bronhouders / 10 geladen / **0
fouten**. Ze zijn dus niet gemist maar pas ná 28-08 in de DSO-lijst verschenen
met een oud registratietijdstip. Vijfde run op rij met dat patroon; het laten
vallen van `--sinds` blijft dragend.

## Stap 1 — Lokaal laden

`full_sync.py --label sync-2026-09-04 --skip-embed` · **4,1 uur · 0 fouten · run_id 12**

| fase | duur | uitkomst |
|---|---|---|
| statistiek-herstel (G-133) | 1,4 min | 153/198 tabellen leeg → 12 hete tabellen geanalyseerd |
| p2p delta-sweep | 26,0 min | 381 bronhouders · **16 geladen = preview** · 0 fout |
| i2a landelijk | 27,9 min | **343/343 ok, 0 fout** — geen enkele 503 (run 11 had er acht) |
| vth load | 13,5 min | 6.293 kennisgevingen |
| **vth verrijking** | **122,0 min** | +6.293 · 0,9/s — de duurste stap van de run |
| afwijkvergunningen (BOPA) | 3,0 min | ok |
| locatie_subdiv | 10,0 min | 14 bronhouders · 563.292 stukjes |
| locatie_generalisatie | 12,2 min | 14 bronhouders · 1.377.568 rijen — eerste beurt binnen een sync |
| vth geometrie-backfill | 5,2 min | +0 (correctiestap; +0 is goed nieuws) |
| indeling | 0,8 min | +114 · beide 0-controles op 0 |
| drieslag-MV's | 14,8 min | geen herhaling van de 2u10m van 21/22-08 |
| health-MV's + ANALYZE | ~4,5 min | |

Regelingen 2.049 → **2.065**, inactief 62 → **73**. DB 93 → 94 GB.
**Regressiecheck: geen afwijkingen.** Rapport:
`dso-loader/scripts/SYNC-REPORT-2026-09-04-sync-2026-09-04.md`.

De preview-vs-uitkomst-check klopt: 16 verwacht, 16 geladen.

## Stap 1b — Wijzigingsspoor

`wijziging ontwerpen`: 1.087 bekeken → **159 relevant**, 928 historisch, 0 fouten.
`wijziging besluiten`: 2.853 bekeken → **6 relevant**, 2.847 historisch, 0 fouten.

Naar prod gespiegeld met `repliceer_p2pwijziging_naar_prod.py --ja`:
10 nieuwe besluiten, 155 gewijzigd, **0 verdwenen** → **1.232.269 rijen over 165
besluiten in 11,8 min** (vingerafdrukken bepalen 111 s voor 499 besluiten).

Citeertitel-controle, beide kanten identiek: besluitversie 141 met **0 zonder
naam** (de eis), ontwerp 358 met 17 zonder — Presenteren levert die niet altijd
en Ontsluiten voegt voor ontwerpen niets toe.

## Stap 2 — Verdrongen versies markeren

0 extra te markeren · 56 vigerende expressies autoritatief via DSO bevestigd · 3
via versie-parse · 0 fouten. De loader had de 11 verdrongen expressies zelf al
gemarkeerd (`markeer_siblings_inactief`); deze stap bevestigt dat.

## Stap 3 — p2p naar prod

`repliceer_p2p_naar_prod.py --ja`: 16 expressies, 41.040 rijen in scope,
**29.435 ingevoegd**, inactief-vlaggen voor 11 works gelijkgetrokken.

**Daarna bleek prod structureel achter te lopen.** `diff_lokaal_prod.py` gaf
**21 AFWIJKINGEN**. Wat daarvan is gedaan:

| | actie | resultaat |
|---|---|---|
| tekstdeel-keten | `backfill_tekstdeel_junctions_prod.py --ja`, drie ronden | +1.239 rijen; `hoofdlijn` aan de keten toegevoegd (zie leerpunt 4) |
| ongerefereerde locaties | 159 alsnog overgezet | `p2p.locatie` gelijk |
| geometriedrift | 8 locaties met **andere geometrie** bij gelijk aantal | bijgewerkt (leerpunt 3) |
| `locatie_subdiv` | 75 bronhouders herbouwd op prod | verschil **+82.385 → +4.631** |
| `locatie_generalisatie` | 63 bronhouders herbouwd (van 99; afgekapt) | verschil **+1.124.556 → +4.362** (99,6%) |
| drieslag-MV's | `refresh_drieslag.py` tegen prod | **35,5 min** |
| health-MV's | via psycopg (psql staat op geen PATH) | bronhouder 29,4 s · geo 8,0 min · ponsenkaart 0,9 s |

**Vooraf aangelegd**: prod miste alle drie de indexen uit
`2026-09-add-generalisatie-prefix-index.sql` (subdiv 181 s, generalisatie 139 s,
planobject 414 s). Zonder die index kost elke bronhouder ~25 s vaste voet.

**Runbook-correctie**: er stond nog *"Nog te doen op prod: daar is op 15-08 320,6
min gemeten"* voor `naammatch_signaal`. Het hardgezette plan zit er wél op — de
log meldt `planner: enable_bitmapscan = off` — en de stap deed **9,9 min**.

## Stap 4 — vth naar prod

`refresh-koop-to-prod.ps1 -Push -Refresh -Verify`: 11.229 rijen ge-upsert,
`VACUUM (ANALYZE)` → visibility map **100%**, drie MV's ververst.
Prod `vth.vergunningkennisgeving` = **911.923** = lokaal.

## Stap 5 — i2a (afweging)

Verschil `i2a.uitvoeringsregel` 1.014.328 lokaal / 1.012.456 prod = **0,18%** →
triviaal, laten staan conform de runbookregel.

Wél gecontroleerd, want de vragenlijst leunt erop: `label` / `toelichting` /
`prioriteit` / `opties` staan op prod op **exact dezelfde dekking** als lokaal
(103.691 / 69.425 / 74.948 / 16.004). De STTR-backfill van 2/3-09 is dus al
overgezet.

`i2a.sttr_bestand` (18.000/0) en `i2a.werkzaamheid_koppel_run` (293/0) zijn met
reden aan `diff_verwachtingen.yml` toegevoegd: de eerste is lokale XML-opslag
waar `vergunningcheck.py` bewust omheen joint, de tweede is bouwboekhouding.

## Stap 6 — Vectorlaag

`refresh-v2a --ja --opruimen`: dirty-set **16 regelingen** (= de geladen set),
**10.917 chunks geëmbed**, 11 verweesde expressies opgeruimd (= de 11 verdrongen).

Daarna de twee lagen die `refresh-v2a` niet aanraakt:
`rebuild_annotatie()` 3,0 min (746.574 rijen) · `extend_categorie()` 4,4 min
(741.370 toewijzingen).

Dekkingscontrole op de nieuwe expressies: **10.917 chunks · 8.211 met categorie
(75%) · 9.869 met annotatie (90%)** — in lijn met de 80%/92% van 13-08.

`ANALYZE v2a.tekst_embedding`: 30 s. De tabel toont nu 1.658.667 levende rijen;
`last_autoanalyze` staat nog steeds op leeg, dus deze stap blijft nodig.

## Stap 6b — Doorwerkingsmeting

`stand.py` meldde **ACHTER** met een signaal dat er op 28-08 nog niet was:
*"omgevingsplannen nooit gescreend: 6"*. De vuistregel uit het runbook zit nu
dus in het gereedschap zelf.

| stap | uitkomst |
|---|---|
| 0a opruimen | 0 verweesde regels |
| 0b verversen | **662 vuile bewijs-sets**; `screening_cel` 88.886 → 11.864, `screening_hit` 167.164 → 62.479 |
| 1a tier1_screen | **54,1 min**, 848 regels — s/regel liep monotoon terug van 9,05 naar 3,94 (koude buffercache na alle herbouw, geen planprobleem) |
| 1b cellen.sql | 89.034 cellen · **13.298 unieke bewijs-sets** · 1.695 regels · 342 gemeenten |
| 1c fase2_fill | 0 werk |
| 1d judge (Sonnet, 3 agents) | **113 sets** — 99,2% hergebruik via de inhoudshash. Verdeling 81 nee / 20 ja / 10 deels / 2 onbepaalbaar |
| 2a doel_screen | 0,7 min · **27 sets** zonder oordeel |
| 2b doel judge | 27 van 27, elk met zijn eigen `systeem`-rol. 20 nee / 4 onbepaalbaar / 3 ja |
| 3 sync_prod | 10.396 geschreven, 8.208 verwijderd; **herhaalde droogloop = 0 verschil op alle vijf tabellen** |

`judge_uniek` 23.522 → **23.662**. `stand.py` daarna **BIJ** op alle zeven
signalen.

**Runbook-correctie**: de waarschuwing dat `doel_screen.py` een afwijkende
werkvoorraad meldt (5.694 tegen 156 op 28-08) is achterhaald — het script
rapporteert nu zelf hetzelfde getal als de export.

## Stap 6bis — Hertaling

2.359 unieke teksten sinds de start van de run, tien Sonnet-subagents.

**Natelling: verwacht 2.359 · gedekt 2.359 · ontbreekt 0** — geen herstelronde
nodig (op 16-08 was die er wél, door een verzonnen sleutel). Cache 26.301 →
**28.660**. Naar prod: 28.660 rijen, MV ververst, 734.412 koppelingen.

Drie van de tien agents probeerden het werk uit te besteden en schreven niets
weg; bijgestuurd, en het sjabloon in `hertaal_fanout_export.py` is aangepast.
Zie leerpunt 12.

## Stap 6c — Onderwerp-as naar prod

`2026-08-06-categorie-naar-productie.py --ja`: 10.917 nieuwe chunks, 10.769 op
prod opgeruimd, **741.370 toewijzingen in 9 s** geladen en omgewisseld,
`chunk_annotatie` 746.574 rijen in 24 s, foreign keys hersteld in 1,6 min.

Daarna `bouw_indeling.py` op prod. **En ook lokaal**: `full_sync` draait die stap
vóór stap 1b, dus de lokale `wijziging_indeling` miste de ontwerpen van deze
nacht (13.335 tegen 14.040 op prod). Na herbouw beide kanten
`artikel_indeling` 149.839/126.077 en `wijziging_indeling` 14.040/9.600.

## Stap 6d — MER-register

Stond **ACHTER**: nieuwste publicatie in de store was 6 augustus, KOOP had tot 2
september. Na de harvest: `event` 2.265, `project` 3.623 (+3), `document` 10.074,
`project_event_link` 449 — prod gelijk aan lokaal op alle vier.
`resolve_bronhouder.py`: 2.760/3.623 projecten gekoppeld (76%).
`harvest/stand.py` daarna **BIJ**.

## Stap 7 — Verificatie

- `preview_sync --vergelijk-prod`: **0 te laden, prod mist er 0**, 0 te embedden.
  De 12 verdwenen zijn de bekende G-91-groep.
- `diff_lokaal_prod.py`: **125 tabellen · 97 gelijk · 28 verwacht verschil · 0
  AFWIJKEND** (was 21 bij de eerste meting).
- `audit.sync_run` run 12 afgesloten op "0 fouten"; geen `load_run` op `running`.
- `/v1/data-health` op prod klopt en komt overeen met het sync-rapport.
- **`/v1/load-status` geeft 500** — databasekant aantoonbaar gezond (hele
  queryreeks slaagt in 12,7 s), fout zit in de draaiende API. Zie leerpunt 13.

## Stap 8 — Downstream

`publish.py --execute`, beide pre-flights groen:

| site | uitkomst |
|---|---|
| ponsenkaart | skip — live, volgt prod |
| instructieregels | ok → `3abeebe5.instructieregels-monitor.pages.dev` |
| annotatieconformiteit | ok → `86b9ad5c.annotatieconformiteit.pages.dev` (7.423 bestanden) |
| mer-register | ok → `4c4bf261.mer-register.pages.dev` |

## Stap 9 — Nazorg

- Lokaal 94 GB, prod 71 GB; geen actieve backends aan beide kanten.
- Geen `VACUUM` nodig: deze run heeft niet geprund (stap 10 niet gedraaid).
- **Nog open**: het `vangnet`-schema van de opruiming van 09-08 staat er nog.
  Het runbook zegt dat het weg mag zodra een sync schoon is verlopen; dat is
  inmiddels meermaals het geval. `DROP SCHEMA vangnet CASCADE;` is een bewuste,
  onomkeerbare handeling en is daarom blijven staan.
- Code van deze run gecommit op `main` (zie hieronder).

## Wijzigingen in de codebase

| bestand | waarom |
|---|---|
| `scripts/full_sync.py` | autocommit vóór de eerste query in `herstel_statistieken_na_herstart()` — de sync viel er anders op om (leerpunt 9) |
| `scripts/backfill_tekstdeel_junctions_prod.py` | `p2p.hoofdlijn` aan `KETEN` toegevoegd; stond alleen als ouder genoemd (leerpunt 4) |
| `scripts/hertaal_fanout_export.py` | opdrachtsjabloon: doe het werk zelf, schrijf per ~40 weg (leerpunt 12) |
| `scripts/diff_verwachtingen.yml` | `i2a.sttr_bestand` en `i2a.werkzaamheid_koppel_run` met reden |
| `docs/sync-2026-09-04-leerpunten.md` | nieuw — dertien punten |
| `docs/sync-2026-09-04.md` | dit bestand |
